# تشغيل آمن وقابل للتوسع — خدمة الأمير

هذه النسخة حسّنت حماية HTTP، تحديد محاولات الدخول، التحقق من حجم JSON، واصطفاف عمليات الكتابة لمنع فقدان التحديثات المتزامنة في التخزين الملفي.

## قبل النشر العام
- استخدم PostgreSQL أو MySQL بدل `data/db.json` عند زيادة عدد المستخدمين.
- شغّل HTTPS عبر Nginx أو Cloudflare.
- اجعل `data/` على قرص دائم مع نسخ احتياطي دوري.
- شغّل أكثر من نسخة للتطبيق فقط بعد نقل التخزين إلى قاعدة بيانات مركزية.
- نفّذ اختبار ضغط على تسجيل الدخول وقراءة/حفظ البيانات.
- أضف صلاحيات دقيقة للمشرفين والخدام قبل تخزين بيانات حقيقية.

## التشغيل
```bash
npm start
```
