const {Pool}=require('pg');
const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:process.env.PGSSL==='true'?{rejectUnauthorized:false}:undefined});
(async()=>{if(!process.env.DATABASE_URL)throw new Error('DATABASE_URL is required');const c=await pool.connect();try{await c.query('BEGIN');await c.query(`CREATE TABLE IF NOT EXISTS app_state (id integer PRIMARY KEY CHECK(id=1), data jsonb NOT NULL, updated_at timestamptz NOT NULL DEFAULT now());`);await c.query(`CREATE TABLE IF NOT EXISTS users (id uuid PRIMARY KEY, phone varchar(20) UNIQUE NOT NULL, password_hash text NOT NULL, role varchar(20) NOT NULL, created_at timestamptz NOT NULL DEFAULT now());`);await c.query(`INSERT INTO app_state(id,data) VALUES(1,$1) ON CONFLICT(id) DO NOTHING`,[{settings:{notifications:true,dark:false,birthday:true,autoSave:true,brightness:100},classes:['أولى إعدادي','ثانية إعدادي','ثالثة إعدادي','أولى ثانوي','ثانية ثانوي','ثالثة ثانوي','خريجين'],members:[],attendance:[],visits:[]}]);await c.query('COMMIT');console.log('PostgreSQL schema is ready');}catch(e){await c.query('ROLLBACK');throw e}finally{c.release();await pool.end()}})().catch(e=>{console.error(e.message);process.exit(1)});
